{
  "mcpServers": {
    "aimemory": {
      "type": "http",
      "url": "${user_config.server_url}",
      "headers": {
        "Authorization": "Token ${user_config.api_key}"
      },
      "enabled": true,
      "timeoutMs": 30000
    }
  }
}
