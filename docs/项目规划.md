# 项目规划（顺序目标清单）

> 2026-09-19 制定（v0.2.0 发布时点）。夜间自动化已结束（本轮成果已推送并打 tag v0.2.0）。
> 规则：**目标不带时间，按序执行**——上一个做完全绿、提交后才开始下一个；每个目标交付时
> 在本文件勾选并附「交付：…」一行。夜间计划（docs/夜间计划-2026-09-19.md）已收官，后续以此文件为准。

## 当前基线（重新评估结论）

- **产品**：四层记忆（L0 归档 / L1 摘要 / L2 消解+向量层 / L3 画像双时间轴）+ MCP 10 工具
  （错误可自愈）+ REST（openapi 契约守护）+ /admin（审计/L3 历史）+ collector（dry-run/.env 模板）
  + 运维件（doctor / smoke / backup / restore / export / import）。
- **质量**：192 项测试全绿；守护四件套（openapi 契约 / env 双向 parity / no-secrets / 检索质量地板）
  + 鉴权行为回归。基线：检索 recall@5=1.0 / MRR=1.0；10 万条 P95=17ms。
- **架构债（唯一）**：`src/db/repo.js` 1154 行，L0/L1/L2/events/keys 杂糅——两次评估确认，现在拆。
- **安全残留**：①LLM 网关 key 轮换（用户动作，历史已泄漏）；②`api_keys.token_plain` 明文列
  （库文件被读=Token 全泄，回看功能可用一次性展示替代）。
- **产品缺口**：生产 embedding 未接入（语义召回未实测）——依赖生产机，本仓库先做「上线准备」。
- **前端**：外部仓库构建中；后端最佳支持 = 类型/示例对接包。

## 目标队列（按序执行，不带时间）

- [x] **G1 架构还债：拆分 src/db/repo.js**
      交付：repo/_common.js（无状态工具）+ repo/{memories,events,l0,l1,keys}.js 五域模块；
      repo.js 1154 行 → 96 行纯 facade（stats 为唯一跨域聚合）。导出面 50 键与拆分前零差异
      （git stash 逐键比对 + 静态提取复核 + 全量测试），分两个 commit（47068c7 阶段 1 / 本提交阶段 2）。
      调用方 60+ 零感知。
- [ ] **G2 前端对接包**：`scripts/gen-api-types.js` 从 `docs/api/openapi.json` 生成 TypeScript 类型
      + `docs/前端对接.md`（REST 鉴权/分页/错误约定 + MCP 握手与工具示例 + 常见场景代码）。
      验收：外部前端按文档可独立完成登录→签发→检索链路。
- [ ] **G3 Token 明文治理**：`api_keys` 去掉 `token_plain` 明文列（校验走 token_hash 不受影响）；
      明文只在**创建响应**返回一次；admin「回看」改为显示掩码（m0-****尾 4 位）；迁移脚本删除存量明文。
      验收：库文件里 grep 不到任何 m0- 明文；旧 Token 全部继续可用。
- [ ] **G4 语义检索上线准备**：embedding 接入 checklist（生产机视角逐步指引）；
      `vec/rebuild` 幂等/断点/维度变更测试补强；embedding 熔断半开恢复路径测试。
      真实语义召回效果验证标注「生产机执行」。
- [ ] **G5 L3 时间推理**：recall_context / 检索排序纳入 `valid_from`（长期失效的约束/画像降权）
      与 `effective_confidence` 联动；测试覆盖过期条目降权。
- [ ] **G6 Docker 部署形态**（可选）：Dockerfile（多阶段，better-sqlite3 编译）+ compose
      （服务端 + 数据卷）+ 部署手册章节；smoke.sh 容器内跑通。
- [ ] **G7 生产实测里程碑**（依赖外部配合）：生产机配 EMBEDDING_* → `POST /api/l2/vec/rebuild`
      → 语义召回实测数字入 docs/检索基线；真实数据回流后四层全链路验收（L0→L1→L2→L3）。

## 执行纪律

1. 顺序执行，不跳项；每个目标完成的标准 = 实现完整 + 测试全绿 + 提交 + 本文件勾选。
2. 架构类改动（G1/G3）每一步小 commit，随时可停在任意中间态。
3. 发现的新工作追加到队列尾部（保持顺序语义）；做完的可选项标注跳过原因。
4. 全程红线不变：密钥不入库不回显；测试零真实 LLM；直写 SQLite 不走素材队列。
