# 成品验收巡检（2026-09-19 02:00 块，08:30 项）

> 方法：起**隔离实例**（临时库 / 临时 L0/L3 目录 / `LLM_ENABLED=0` / `EMBEDDING_ENABLED=0` /
> 测试口令 / 端口 18999），真实 HTTP 走完全部人工路径；生产数据与真实 `.env` 零接触。
> 附带**剪枝复评**：验证剪枝提交（6940298 / 421b1b2 / ccea49f）未破坏任何路径。

## 一、Web / REST 路径（全部通过）

| 检查 | 结果 |
|---|---|
| `GET /healthz` | ✅ `{"status":"ok","db":true,"embedding":"disabled","llm":"disabled"}` |
| `GET /`（根路径由管理台承接） | ✅ 200 text/html |
| `GET /admin` | ✅ 200 |
| `GET /auth/login`（口令表单） | ✅ 200 |
| 登录：错口令 → 401，正口令 → 302 建会话 | ✅ |
| `GET /api/stats` / `/api/me`（cookie 鉴权） | ✅ 身份 owner/我，via session |
| `GET /api/atlas/overview`（剪枝后保留的聚合接口） | ✅ 四层统计完整 |
| `GET /api/openapi.json` | ✅ 200 |
| `GET /api/l1/summaries` / `/api/l0/stats` / `/api/l3/entries`（空库） | ✅ 结构完整 |
| `POST /api/keys` 签发 Token | ✅ 201 返回 `token`（明文仅此一次响应） |
| `GET /skill/download`（zip）/ `/skill/SKILL.md` | ✅ 200 |
| `GET /icon-32.png`（admin 静态兜底） | ✅ 200 |
| 剪枝复评：`/samples/*` 与 `/atlas/*` | ✅ 均如预期 404（挂载已随剪枝移除） |

## 二、MCP（Streamable HTTP，完整握手）全部通过

握手流程：`initialize`（协议 2025-03-26，server aimemory v0.2.0）→ `notifications/initialized`
→ 携 `mcp-session-id` 调用。curl 裸调会依次遇到 `Accept` 头与「未初始化」两层规范约束——**均为协议正确行为**。

| 检查 | 结果 |
|---|---|
| `tools/list` | ✅ 10 个工具，含 title，顺序确定 |
| `search_memories`（空库） | ✅ 正常结果 `{"results":[]}` |
| `add_memory`（LLM 关闭） | ✅ **isError 执行错误**：指向 `LLM_ENABLED` 配置 + `search_memories` 替代动作 |
| `list_session_summaries` / `recall_context`（空库） | ✅ 三类分组 + facts 空结构完整 |
| `search_memories` 空 query | ✅ isError + 修正建议 |
| `get_memory` 不存在 id | ✅ isError + 「先 get_memories 列出有效 id」 |

## 三、巡检发现并当场修复

1. **add_memory 在 `LLM_ENABLED=0` 时返回协议错误（-32603）**——违反 04:45 定下的
   「业务错误 → isError 执行错误」规范。已修（697e8a4）：MCP 层预检 + 可自愈文案 + 回归测试。
2. 验收脚本两处自身笔误（`/api/keys` 响应字段为 `token` 非 `key`；缺 MCP 规范要求的
   `Accept` 头与初始化握手）——非服务缺陷，修正脚本后复跑。

## 四、剪枝复评结论

- 静态路径：`/` 由管理台承接、`/admin` 正常、剪枝目录的 URL 均按预期 404，无残留死链（admin 侧边栏已清理）。
- 保留项工作正常：`/api/atlas/overview`、检索语料、icons（当时归 `src/web/static/`，现已随前端重构移到 `web/public/`）。
- 工作区 `grep` 复核：除「已剪枝」说明性文字与历史台账外，无代码引用 atlas/samples。

## 五、测试与遗留

- 全量 `node --test`：**174 项全绿**。
- 遗留观察项（不阻塞成品）：
  - 语义检索真实效果待生产 embedding 接入后 `POST /api/l2/vec/rebuild` 验证（本机无 embedding 服务）。
  - **安全：历史推送中含旧 LLM 网关 key（08-22 起），需用户轮换**（docs/复盘-2026-09-19-剪枝.md §三）。

## 六、同日追加：管理台前端重构验收（G9）

> 方法：本机 `node src/index.js`（18543）+ `web/dist` 真实构建产物，**真实浏览器**逐视图点验；
> 数据用真实链路灌入（`POST /api/l0/ingest` 归档 → L1 摘要 → L2 派生），未走任何 mock。
> **限制**：in-app browser 视口 0×0，截图与指针事件不可用——改为 DOM 结构 + `getComputedStyle`
> 断言（背景色/圆角/侧栏宽度等令牌确实生效），像素级观感未核对；窄屏媒体查询（≤880px）被强制命中，
> 侧栏呈 64px 图标态、导航文字隐藏，属预期响应式行为而非缺陷。

| 路径 | 结果 |
|---|---|
| 会话探测 → 未登录显示口令表单 / 已登录进应用壳 | ✅ |
| Token：新建 → 一次性明文 + 「复制 JSON（含 Token）」；手动模式四字段；吊销 → 列表移除 + 文案更新 | ✅ |
| 无 Token 时 MCP JSON **不含 `headers` 键**、按钮转为「复制 JSON 模板」（G3 语义） | ✅ |
| 会话归档四级下钻：设备 → agent → 会话 → 详情；切设备清空下级；agent 间会话不串台 | ✅ |
| 详情页：角色分色条目、`原始` JSON 展开/收起、L1 摘要 **done**（概述/关键决定/产出物）与 **failed**（含原因）两态、`关闭` 收起 | ✅ |
| 记忆：提交素材 → 轮询至 done（「提炼完成，新增 2 条」）→ 列表刷新（**返回≠入库**语义可视化）| ✅ |
| 记忆：编辑（PATCH + 「已更新」）、删除（确认 + 「已删除」）、搜索 3 命中 / 清除还原、导出下载 | ✅ |
| 分页：12 条 → 1/2 页 10 条，翻页后 2/2 页 2 条且「下一页」禁用 | ✅ |
| 操作审计：12 行（ADD/NOOP 徽标、变更截断、来源 `l1:codex/…`、时间紧凑格式） | ✅ |
| L3：条目列表 + 被取代项删除线且不给编辑按钮、统计行、变更历史链（现行 ← 旧版）、正文编辑 PUT 落盘 | ✅ |
| 接入指南：技能下载两链接、MCP 步骤、工具一览覆盖实际 **10** 个工具 | ✅ |
| 主题切换 → localStorage 持久 → 刷新后首帧即为已选主题（无闪烁） | ✅ |
| 浏览器控制台：全流程 **零 error / 零 warn** | ✅ |

- 回归：全量 `node --test` **211 项全绿**（含接替旧 DOM-stub 的 `test/web-logic.test.js` 10 项）；
  `scripts/smoke.sh` **11/11**；`npm run doctor` 新增「管理台前端」项 ✅；`/admin`、`/`、
  哈希资源（js 257 KB / css 19 KB）与图标均 200。
- 验收中顺带修正的旧页问题（详见 G9 条目）：外部构建结论、"明文可随时复制"文案、工具数 7 → 10、
  `keyToken` 未声明的 latent bug、`form` 只有 `id` 导致 `.login-form` 样式从未生效。
- **Docker 镜像实测**：web 构建阶段首轮失败——前端要 import 仓库根的 `docs/api/aimemory-api.ts`，
  而阶段内只 COPY 了 `web/`（连带 5 个 TS 报错，全为同一根因）。改为镜像内复刻仓库目录形状
  （`/app/web` + `/app/docs/api`）后重建通过；起一次性容器（空数据卷 + `LLM_ENABLED=0` + 随机口令）
  从宿主打映射端口跑 smoke **11/11**，`/admin` 与哈希产物（js 257 KB / css 19 KB）均 200，
  文件名与本地构建一致。另记：运行镜像内没有 curl，smoke 只能从宿主打（部署文档 §2.5 已按此改措辞）。
- 验证用临时数据已清理：一次性 Token 全部吊销、5 条分页临时记忆与 3 条 L3 试写条目删除、
  伪造的 L1 状态行删除（真实 LLM 生成的摘要与派生记忆保留）。`data/` 全程 gitignored。
