# 复盘：整体评估与剪枝（2026-09-19 02:00 块）

> 授权来源：用户 00:40「该剪枝就剪」+ 02:00「当前的星云圆圈的前端我不会采用，集中精力把核心做好」。
> 执行原则：先记录 → grep 确认无引用 → 测试全绿 → 独立小 commit → 以删除与简化为主，不做大规模重写。

## 一、剪枝清单

| # | 对象 | 规模 | 理由 | 影响 | commit |
|---|---|---|---|---|---|
| 1 | `atlas/` 星图前端 | 208K / 14 文件 / -5268 行 | 用户明示不采用；前端由外部仓库另行构建 | `/` 由 `/admin` 管理台承接；`/admin` 路由移出 atlas 存在性守卫；admin 侧边栏星图死链移除 | 421b1b2 |
| 2 | `samples/` 前端样例 | 2.2M / -53.5K 行（含 vendor） | three/vue/preact 评估样例使命结束（前端定论为外部构建） | `/samples` 挂载移除 | ccea49f |
| 3 | `gui-test-screenshots/` | 6.6M / 41 文件 | 浏览器 GUI 测试截图产物，零引用，误入库 | 加入 `.gitignore` 防再次入库 | ccea49f |
| 4 | `src/config.js` 硬编码 `LLM_API_KEY` 默认值 | 1 行 | 安全止血（见下「安全事件」） | 密钥只从 `.env` 读取 | 6940298 |

**合计：72 文件，+76 / -58,998 行**（`git diff --shortstat 87bf148..HEAD`），测试 170/170 全绿。

## 二、保留决策（评估过但不剪）

| 对象 | 理由 |
|---|---|
| `GET /api/atlas/overview` 聚合接口 | 后端接口（非前端代码）；openapi 已文档化，外部新前端可能已按此对接。改名=破坏性变更，无收益 |
| 检索语料 `atlas-webgl` 条目（test/fixtures/retrieval-cases.json） | 只是检索测试语料文本，不是代码引用；改动会漂移检索基线数字 |
| `src/web/static/icon-*.png` | 归 admin 管理台所有（favicon/PWA），与 atlas 无关 |
| 四层分层（src/l0…l3 / repo / web） | 职责边界清晰、无双头实现；L2 四操作与 L3 双时间轴是 mem0/letta 验证过的最小面。**「不动架构」本身就是本轮结论** |

## 三、安全事件记录（需用户处置）

1. **LLM 网关 API key 曾硬编码在 `src/config.js`**：2026-08-22（4aad856a）引入，
   已进入 `origin/main` 与 `origin/feat/memory-atlas` 的**推送历史**。
   2026-09-19 已从工作区移除（6940298）并新增 `test/no-secrets.test.js` 回归守护
   （扫描 src/collector/scripts 的凭据形态字符串 + `.env` 不入库 + 模板密钥留空）。
2. **`.env`（含 KEYCLOAK 凭据 / SESSION_SECRET / 各服务 key）曾入库**：d108202e 引入、138b250a 停止跟踪，但同样在推送历史中。

**处置建议（按优先级）**：
- ✅ **轮换凭据（唯一彻底方案）**：作废并重发该 LLM 网关 key；Keycloak 已随多租户裁剪移除，其凭据随实例下线即可。
- ⚠️ 历史重写（BFG / filter-repo）仅在「仓库曾公开或将公开」时才值得做：需要 force push 并重克隆所有工作副本；
  本仓库为个人自托管私有仓库，轮换已足够。

## 四、死代码 / 重复实现盘点

- 夜间各条目「即写即测」，01:22 已盘点关闭一项伪需求（L1 全文搜索重复建设）；本轮 grep 复核
  `src/` 导出函数无新增孤儿（每个 store/repo 导出均有调用方或测试覆盖）。
- 剪枝后 `src/index.js` 静态挂载简化为 `/admin` 路由 + 单个 `web/static` 挂载，无残留守卫。

## 五、剪枝后的产品形态

- **核心面**：MCP（`/mcp`，10 工具）+ REST（`/api/*`，openapi 契约守护）+ `/admin` 管理台 + `collector/` 采集器。
- **前端**：由用户外部仓库对接（`/admin` 保留为兜底管理面）。
- 恢复方式：`git log --all -- atlas/ samples/` 任意历史版本可整目录找回。
