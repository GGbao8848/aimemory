# 夜间自动开发计划（2026-09-19 01:00 → 09:00）

> 本文件是夜间定时任务的执行清单：每 30 分钟起一个 ZCode 会话，每轮**只做一个条目**，
> 自上而下取第一个未勾选项。做完了就勾掉并附一行交付说明；单项 30 分钟做不完就交付
> 最小可用部分并如实标注「部分完成」。目标：09:00 时全部完成，后端达到「成品」状态。
>
> **红线（每轮必须遵守）**
> 1. 绝不读取、打印、提交 `.env` 或任何密钥/令牌；LLM 相关验证一律用 stub，夜间零真实 LLM 调用。
> 2. 开发模型使用 glm flash。
> 3. 每完成一个条目 `git commit`（仓库 conventional 中文风格），**不 push**（最后一项 09:00 统一验收后 push）。
> 4. 收尾前 `node --test` 必须全绿；上一条目留了红测试先修复再前进。
> 5. 时间到点未完成 → 砍范围交付最小可用，宁可「部分完成」不留坏提交。
> 6. **DeepSeek key 适量食用（用户 00:15 追加）**：压测/导入/导出/备份脚本一律**直写 SQLite**，
>    严禁经过 `add_memory` → `events` 队列或任何会触发 LLM 提炼/消解/凝练的路径；
>    起服务做验收时用 `LLM_ENABLED=0` 起一个隔离实例，或确认不会向其投喂任何素材。

## 执行清单

- [ ] **01:00 L3-MCP 注入**：新增 MCP 工具 `recall_context`（只读）：返回 L3 active 条目（按 kind 分组）+
      可选的 top-K 高相关 L2 事实（关键词命中），供 agent 开场直接注入画像/约束上下文。
      验收：MCP 工具清单 +1，工具描述写明用途；测试覆盖 user 隔离与 LLM 关闭时降级。
- [ ] **01:30 L3 置信度衰减**：`confidence` 增加只读的时效衰减视图（指数半衰，默认半衰 180 天，
      `L3_HALF_LIFE_DAYS` 可调），`/api/l3/entries` 与 stats 输出 `effective_confidence`；文件原值不动。
      验收：衰减纯函数 + 单测（边界：新建=原值、极旧→趋近 0）。
- [ ] **02:00 L2→L3 深度参与**：L3 凝练输入加入「L2 事实采样」（最近更新的 active 记忆 top-N，
      N≤10、单条裁剪 80 字，token 预算内），提示词声明"仅作背景，不直接抄进条目"。
      验收：derive 测试更新；prompt 构建 tests 断言采样存在且被裁剪。
- [ ] **02:30 REST API 契约文档**：`docs/api/openapi.yaml`（或 .json）覆盖 L0/L1/L2/L3/healthz 全部接口 +
      `GET /api/openapi.json` 直出；加一个一致性测试：扫 express 路由表断言文档无遗漏（允许显式豁免清单）。
- [ ] **03:00 备份与恢复**：`scripts/backup.sh`（sqlite `.backup` + data/l0、data/l3 增量打包 + 保留 N 份）与
      `scripts/restore.sh`（校验和验证 + 恢复 + 启动自检）；docs/运维手册补「备份/恢复」章。
      验收：在临时目录演练一轮备份→破坏→恢复→测试全绿。
- [ ] **03:30 全量导出/导入**：`scripts/export.js`（memories/l1_summaries/l3 条目/l0 索引 → 单包 JSON+清单+校验和）
      与 `scripts/import.js`（幂等：同 id 跳过、冲突可选覆盖）；CLI 参数与 README 用法。
      验收：导出→清库→导入→测试全绿（用临时库）。
- [ ] **04:00 检索质量评测集**：`test/fixtures/retrieval-cases.json`（25+ 条中文 query→期望命中 id/关键词）+
      `scripts/eval-retrieval.js` 输出 recall@k / MRR 基线报告（docs/检索基线-2026-09-19.md）。只建度量，不改算法。
- [ ] **04:30 检索小步改进**：依据 04:00 基线做**一项**低风险改进（候选：中文双字词 bigram 索引、
      facts/entities 字段参与 bm25、短词兜底扫描排序优化），评测集对比不回退方可合入。
- [ ] **05:00 性能压测基线**：`scripts/bench-search.js` 生成 1 万/10 万条合成记忆，分别压
      `search_memories`（关键词路径与向量路径分开），输出 P50/P95 表格入 docs/性能基线-2026-09-19.md。
      验收：脚本可重复跑、结论明确（是否需要索引参数调整）。
      ⚠️ 合成数据必须直写 SQLite（复用 src/l2/store.js 的 insertFact 或裸 SQL），绝不走 add_memory 队列。
- [ ] **05:30 生产部署手册**：`docs/部署-生产机.md`：pm2 配置、.env 模板逐项说明、首次部署步骤、
      升级与回滚、备份 cron 示例、故障排查（对照仓库真实文件写，不写架空内容）。
- [ ] **06:00 /admin 审计与 L3 页**：旧管理台新增「记忆操作审计」（memory_ops 最近 50 条，含 before/after）
      与「L3 条目」页（列表 + 正文编辑，走 /api/l3/*）。改动仅限 src/web/static/，不碰 atlas/。
- [ ] **06:30 星图检视增强**：atlas 的 L3 节点检视面板拉取 `/api/l3/entries` 展示条目列表（kind 分组 + 正文）；
      memories 表节点检视追加四操作计数（已有字段，补渲染）。改动仅限 atlas/js/ui/inspector.js 与 topology.js。
- [ ] **07:00 全量测试补强**：通读夜间新增代码，补缝隙（schema 迁移幂等、错误处理、边界）；补一条
      「备份→恢复」与「导出→导入」的端到端测试；全量必须全绿。
- [ ] **07:30 首启体验**：首次启动自检清单输出（依赖、目录权限、LLM/embedding 探活结果、生成口令提示），
      缺失配置给出一行式修复建议；`.env.example` 与 config.js 逐项核对补漏。
- [ ] **08:00 文档总对齐**：README / 四层架构 / L2 / L3 文档与代码逐项核对（接口表、env 参数表、台账补夜间接项），
      修正过时表述；samples/ 前端样例的 README 也一并对齐。
- [ ] **08:30 成品验收巡检**：起真实服务走一遍人工路径（登录 / 星图 / admin / samples / MCP 工具清单 / 各 REST），
      产出 `docs/验收-2026-09-19.md`（通过项/发现问题）；发现小问题当场修，大问题记录不硬修。
- [ ] **09:00 收尾推送**：全量测试全绿 → 汇总 commit（若有未提交项）并 `git push` 到 feat/memory-atlas；
      若有红测试：修到绿再推，修不绿则**不推**并在最终输出里明确列出失败项。

## 候补清单（计划被打乱时按此动态替换/插队）

- `search_memories` 支持按 kind/scope 过滤（如只搜 constraints）
- L2 `update_memory` 后触发一次局部重消解（与邻居事实比对）
- 采集器：会话静默判定参数化文档化 + `collector --dry-run`
- `/api/l1/summaries` 增加全文搜索（找"哪个会话改过 sqlite-vec"）
- L3 条目变更历史视图（被取代链可视化，纯读）
- 事件队列积压告警阈值与 /healthz 暴露

## 完成定义（成品线）

`node --test` 全绿；四层各有可用出口（MCP/REST/前端至少其一）；备份可恢复、导出可导入；
有检索与性能基线数字；部署手册可照做；文档与代码一致；全部提交并推送。
