# 生产机部署手册

> 目标机器：一台常驻的内网服务器（Linux）。本文只写本仓库真实存在的文件与行为，不架空。

## 1. 前置要求

| 项 | 要求 | 说明 |
|---|---|---|
| Node | ≥ 20（建议 22 LTS） | `engines: >=20` |
| pm2 | 任意近期版本 | `npm i -g pm2`；`ecosystem.config.js` 已配好（`max_memory_restart: '512M'`） |
| 外网 | 仅 `npm install` 时需要 | 运行时不拉任何外链（前端零 CDN）；内网机可离线装（把 node_modules 一起拷过去） |

## 2. 首次部署（10 分钟）

```bash
git clone https://github.com/GGbao8848/aimemory.git && cd aimemory
git checkout feat/memory-atlas        # 或主分支
npm install                           # better-sqlite3 需编译；sqlite-vec 是 optional 依赖，装不上不阻塞
cp .env.example .env
```

编辑 `.env`，**必看项**（全部有默认值，但生产建议逐项确认）：

| 变量 | 说明 |
|---|---|
| `PORT` | 单端口承载 /mcp + /v1 /v2 + Web，默认 18543 |
| `AIMEMORY_PASSWORD` | Web 登录口令；**留空则首次启动自动生成并写回 .env**（启动日志会打印一次） |
| `LLM_ENABLED` / `LLM_BASE_URL` / `LLM_MODEL` / `LLM_API_KEY` | 素材提炼/消解判定必需；任意 OpenAI 兼容 chat/completions 端点 |
| `EMBEDDING_ENABLED` | 语义召回可选；`0` 时检索自动退关键词（功能完整，召回少一路） |
| `L2_*` | 冲突消解的开关与 token 预算，默认全开（见 .env.example 注释） |
| `AIMEMORY_DB` | 数据库落盘位置覆盖（默认 `data/aimemory.db`；一般无需动） |

```bash
npm install                           # 后端依赖
npm run web:install && npm run web:build   # 管理台前端（web/ → 产物 web/dist）
npm run doctor                        # 首启自检：依赖/目录/数据库/前端产物/LLM 配置，缺什么给一行修复命令
pm2 start ecosystem.config.js && pm2 save
scripts/smoke.sh                      # 部署冒烟：healthz/登录/Token/MCP 握手/检索一条命令验完（失败非 0 退出码）
curl http://127.0.0.1:18543/healthz   # {"status":"ok","db":true,...}
```

浏览器打开 `http://<服务器>:18543/admin`（管理台，口令登录；同时挂在 `/`）。前端为本仓 `web/`
（Vite + React + TS）的构建产物，由后端直接托管——**未构建也能起服务**，只是 `/admin` 返回 503 与一行提示。
星图/样例前端已于 2026-09-19 剪枝；外部 agent 只对接 `/api/*` 与 `/mcp`。

### 语义检索（embedding）接入指引

未接入时检索走关键词路径（FTS + OR 兜底，功能完整），接入后多一路语义召回（同义/口语化可命中）：

1. 准备任意 OpenAI 兼容 `/v1/embeddings` 服务（vLLM / llama.cpp / 网关均可），记下地址、模型名、密钥（可选）。
2. `.env` 五项：`EMBEDDING_ENABLED=1`、`EMBEDDING_BASE_URL`、`EMBEDDING_MODEL`、`EMBEDDING_API_KEY`、
   `EMBEDDING_TIMEOUT_MS`（可选：`EMBEDDING_BREAK_THRESHOLD` / `EMBEDDING_RETRY_MS` 调熔断）。
3. 重启服务 → `npm run doctor -- --probe` 看「Embedding 探活」是否 ok。
4. 为存量记忆回填向量：`npm run vec:rebuild`（幂等，可重复跑）；**换 embedding 模型（维度变了）**
   必须加 `-- --reset` 重建索引。
5. 验证：`search_memories` 用一条同义改写查询（如库里是"部署端口 18543"，查"服务跑在哪个口"）能命中即为生效。
6. 回退：任何时候 `EMBEDDING_ENABLED=0` 重启即回纯关键词，数据无损；服务抖动有半熔断自动降级/恢复
   （`/healthz` 的 `embedding` 字段可见探活状态）。

## 2.5 Docker 部署形态（可选，2026-09-19）

不想装 Node/pm2 的机器可以用容器：

```bash
cp .env.example .env                # 配置与 pm2 部署完全同一份模板

```bash
cp .env.example .env                # 配置与 pm2 部署完全同一份模板
docker compose up -d --build        # 构建 + 启动；数据落 ./data 卷
scripts/smoke.sh                    # 冒烟从宿主打映射端口（镜像内无 curl）：11 项实测通过
docker compose logs -f              # 看启动日志（含自动生成口令）
```

要点：容器内端口固定 18543（`environment.PORT` 覆盖 .env 的 PORT，外部映射在 compose 里改）；
数据卷 = `./data`（db），备份/恢复沿用 scripts/backup.sh（把路径指向该目录即可）；
升级 = `git pull && docker compose up -d --build`（旧库迁移自动执行，含 mem0 收敛的退役表 DROP）。
镜像以非 root 用户运行，better-sqlite3 原生编译隔离在 build 阶段；管理台前端在独立的 `web` 构建阶段
产出（`web/dist`，含 `docs/api` 契约类型，故该阶段目录形状与仓库一致），宿主机无需任何 Node 工具链；
sqlite-vec 拉不到 prebuild 时自动降级关键词检索，不阻塞构建。

## 3. agent 接入（MCP / REST）

`/admin` 提供接入配置 JSON（Streamable HTTP）。核心配置形如：

```json
{ "mcpServers": { "aimemory": { "url": "http://<服务器>:18543/mcp", "headers": { "Authorization": "Token m0-xxx" } } } }
```

agent 拿到的 MCP 工具共 7 个：add_memory / get_event_status / search_memories / get_memories /
get_memory / update_memory / delete_memory。机器到机器的 REST 接入走 mem0 形态 `/v1` `/v2` 面
（用法见 [前端对接.md](前端对接.md) 或 README）。

## 5. 升级

```bash
cd aimemory
git pull
npm install          # 依赖有变时
pm2 restart aimemory-mcp
node --test          # 全量测试（契约一致性由 test/api-contract.test.js 守护）
```

schema 迁移全部幂等（启动时自动补列/建表），老库直接跑。

## 6. 回滚

```bash
git log --oneline -5                # 找上一个可用提交
git checkout <commit>
npm install && pm2 restart aimemory-mcp
```

数据回滚用备份（见 [运维手册](运维手册.md)）：**每次升级前跑一次 `scripts/backup.sh`**。

## 7. 备份

```bash
# crontab -e（每天 03:00，保留 14 份）
0 3 * * * /path/to/aimemory/scripts/backup.sh /path/to/data /path/to/backups 14
```

备份即 SQLite 在线快照，异地同步 `backups/` 目录即可。

## 8. 故障排查

| 症状 | 排查 |
|---|---|
| 进程反复重启 | `pm2 logs aimemory-mcp`；历史上是冷启动内存超限（采集器已改流式）；必要时调大 `max_memory_restart` |
| LLM 相关功能失效 | 服务有**半熔断**：连续失败自动熔断、每 60s 探测自动恢复，无需重启；`/healthz` 看 llm 字段 |
| 检索只有关键词结果 | embedding 不可用会自动降级（healthz 的 embedding 字段）；接上后 `npm run vec:rebuild` |
| 端口被占 | `lsof -i :18543`；改 `.env` 的 PORT |
| 忘记 Web 口令 | 删 `.env` 里的 `AIMEMORY_PASSWORD` 行并重启 → 自动生成新口令打印在日志 |

## 9. 安全

- `data/` 含记忆库（个人数据），**目录权限等同密钥**；`.env` 已被 gitignore，密钥绝不入库。
- 数据库单文件 + WAL，随 `data/` 一起备份。
- 服务只暴露一个端口；如需公网访问，前面加反代并自行加 TLS。
